services:
  mysql:
    image: mysql:8.4
    container_name: laravel-video-uploader
    restart: unless-stopped
    environment:
      MYSQL_DATABASE: video_uploader
      MYSQL_ROOT_PASSWORD: root
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
